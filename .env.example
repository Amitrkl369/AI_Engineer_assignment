# Copy to .env and set secrets
OPENAI_API_KEY=
# Optional: path to tesseract executable if not on PATH
TESSERACT_CMD=
# LLM provider: 'openai' or 'gemini'
LLM_PROVIDER=gemini
# If using Gemini via a generic HTTP endpoint, set these:
GEMINI_API_KEY=
GEMINI_ENDPOINT=
